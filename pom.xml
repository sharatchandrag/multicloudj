<?xml version='1.0' encoding='UTF-8'?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <groupId>com.salesforce.multicloudj</groupId>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>multicloudj-parent</artifactId>
    <packaging>pom</packaging>
    <description>Salesforce Multi-substrate Java SDK</description>
    <version>0.5.0</version>
    <name>multicloudj</name>
    <url>https://github.com/salesforce/multicloudj</url>

    <licenses>
        <license>
            <name>The Apache License, Version 2.0</name>
            <url>https://github.com/salesforce/multicloudj/blob/main/LICENSE.txt</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>MultiCloudJ Developers</name>
            <email>opensource@salesforce.com</email>
            <organization>Salesforce</organization>
            <organizationUrl>https://www.salesforce.com</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/salesforce/multicloudj.git</connection>
        <developerConnection>scm:git:ssh://github.com:salesforce/multicloudj.git</developerConnection>
        <url>https://github.com/salesforce/multicloudj</url>
    </scm>

    <modules>
        <module>iam</module>
        <module>blob</module>
        <module>dbbackuprestore</module>
        <module>docstore</module>
        <module>examples</module>
        <module>pubsub</module>
        <module>registry</module>
        <module>sts</module>
        <module>multicloudj-common</module>
        <module>multicloudj-common-ali</module>
        <module>multicloudj-common-aws</module>
        <module>multicloudj-common-gcp</module>
        <module>coverage-report</module>
    </modules>
    <properties>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <compileSource>17</compileSource>
        <releaseTarget>17</releaseTarget>
        <maven.min.version>3.5.0</maven.min.version>
        <java.min.version>${compileSource}</java.min.version>
        <slf4j.version>2.0.15</slf4j.version>
        <commons-logging.version>1.3.4</commons-logging.version>
        <guava.version>33.2.1-jre</guava.version>
        <guice.version>7.0.0</guice.version>
        <junit.version>5.12.1</junit.version>
        <mockito.version>5.18.0</mockito.version>
        <wiremock.version>3.13.2</wiremock.version>
        <lombok.version>1.18.46</lombok.version>
        <auto-service.version>1.1.1</auto-service.version>
        <commons-lang3.version>3.20.0</commons-lang3.version>
        <httpclient.version>4.5.14</httpclient.version>
        <httpcore.version>4.4.16</httpcore.version>
        <httpasyncclient.version>4.1.5</httpasyncclient.version>
        <reactive-streams.version>1.0.4</reactive-streams.version>
        <eventstream.version>1.0.1</eventstream.version>
        <jmh.version>1.37</jmh.version>
        <netty.version>4.1.136.Final</netty.version>
        <jackson.version>2.19.0</jackson.version>
        <aws-sdk.version>2.42.40</aws-sdk.version>
        <commons-codec.version>1.16.0</commons-codec.version>
        <wiremock-grpc.version>0.11.0</wiremock-grpc.version>
        <opentelemetry.version>1.62.0</opentelemetry.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <revision>0.2.5-SNAPSHOT</revision>
        <!-- benchmarks jar off by default: 'none' leaves the inherited jar execution unbound from the lifecycle (intentional disable, not a typo). A benchmark-bearing module opts in by overriding this to 'package'. -->
        <benchmarks.jar.phase>none</benchmarks.jar.phase>
    </properties>
    
    <repositories>
        <repository>
            <id>central</id>
            <name>Maven Central Repository</name>
            <url>https://repo.maven.apache.org/maven2</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>
    
    <!-- All module dependencies and versions -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.inject</groupId>
                <artifactId>guice</artifactId>
                <version>${guice.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.inject.extensions</groupId>
                <artifactId>guice-assistedinject</artifactId>
                <version>${guice.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.inject.extensions</groupId>
                <artifactId>guice-multibindings</artifactId>
                <version>${guice.version}</version>
            </dependency>

            <!-- Testing Framework -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-api</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-engine</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-params</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-junit-jupiter</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.wiremock</groupId>
                <artifactId>wiremock</artifactId>
                <version>${wiremock.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.wiremock</groupId>
                <artifactId>wiremock-grpc-extension</artifactId>
                <version>${wiremock-grpc.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- Build Tools -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>com.google.auto.service</groupId>
                <artifactId>auto-service</artifactId>
                <version>${auto-service.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.auto.service</groupId>
                <artifactId>auto-service-annotations</artifactId>
                <version>${auto-service.version}</version>
            </dependency>

            <!-- Commons Libraries -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-logging</groupId>
                <artifactId>commons-logging</artifactId>
                <version>${commons-logging.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-codec</groupId>
                <artifactId>commons-codec</artifactId>
                <version>${commons-codec.version}</version>
            </dependency>

            <!-- HTTP Client -->
            <dependency>
                <groupId>org.apache.httpcomponents</groupId>
                <artifactId>httpclient</artifactId>
                <version>${httpclient.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.httpcomponents</groupId>
                <artifactId>httpcore</artifactId>
                <version>${httpcore.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.httpcomponents</groupId>
                <artifactId>httpasyncclient</artifactId>
                <version>${httpasyncclient.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.httpcomponents</groupId>
                <artifactId>httpcore-nio</artifactId>
                <version>${httpcore.version}</version>
            </dependency>

            <!-- Reactive Streams -->
            <dependency>
                <groupId>org.reactivestreams</groupId>
                <artifactId>reactive-streams</artifactId>
                <version>${reactive-streams.version}</version>
            </dependency>

            <!-- AWS Event Stream -->
            <dependency>
                <groupId>software.amazon.eventstream</groupId>
                <artifactId>eventstream</artifactId>
                <version>${eventstream.version}</version>
            </dependency>

            <!-- OpenTelemetry (observability: spans + context propagation) -->
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-api</artifactId>
                <version>${opentelemetry.version}</version>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-context</artifactId>
                <version>${opentelemetry.version}</version>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-sdk-testing</artifactId>
                <version>${opentelemetry.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- Performance Benchmarking -->
            <dependency>
                <groupId>org.openjdk.jmh</groupId>
                <artifactId>jmh-core</artifactId>
                <version>${jmh.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjdk.jmh</groupId>
                <artifactId>jmh-generator-annprocess</artifactId>
                <version>${jmh.version}</version>
            </dependency>

            <!-- Netty -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-buffer</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-codec</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-codec-http</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-codec-http2</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-common</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-handler</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-resolver</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-transport</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-transport-classes-epoll</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-transport-native-unix-common</artifactId>
                <version>${netty.version}</version>
            </dependency>

            <!-- Jackson JSON Processing -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.dataformat</groupId>
                <artifactId>jackson-dataformat-cbor</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.dataformat</groupId>
                <artifactId>jackson-dataformat-xml</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.dataformat</groupId>
                <artifactId>jackson-dataformat-yaml</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.datatype</groupId>
                <artifactId>jackson-datatype-jdk8</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.datatype</groupId>
                <artifactId>jackson-datatype-jsr310</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.module</groupId>
                <artifactId>jackson-module-jakarta-xmlbind-annotations</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.module</groupId>
                <artifactId>jackson-module-parameter-names</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <!-- Internal MultiCloudJ Dependencies -->
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>multicloudj-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>multicloudj-common</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>multicloudj-common-aws</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>multicloudj-common-aws</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>multicloudj-common-ali</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>multicloudj-common-ali</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>multicloudj-common-gcp</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>multicloudj-common-gcp</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>sts-client</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>sts-client</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>iam-client</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>iam-client</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>blob-client</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>blob-client</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>docstore-client</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>docstore-client</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>dbbackuprestore-client</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>dbbackuprestore-client</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>pubsub-client</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>pubsub-client</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>registry-client</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.salesforce.multicloudj</groupId>
                <artifactId>registry-client</artifactId>
                <version>${project.version}</version>
                <type>test-jar</type>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.4.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>3.4.0</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>integration-test</goal>
                                <goal>verify</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>3.11.2</version>
                    <configuration>
                        <sourcepath>${project.build.directory}/generated-sources/delombok</sourcepath>
                        <doclint>none</doclint>
                        <quiet>true</quiet>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.projectlombok</groupId>
                    <artifactId>lombok-maven-plugin</artifactId>
                    <version>1.18.20.0</version>
                    <configuration>
                        <addOutputDirectory>false</addOutputDirectory>
                        <sourceDirectory>src/main/java</sourceDirectory>
                    </configuration>
                    <dependencies>
                        <dependency>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </dependency>
                    </dependencies>
                </plugin>
                <!-- benchmark-only artifact: benchmark classes + JMH-generated classes + JMH metadata (BenchmarkList, CompilerHints), no test infra. A module opts in by setting benchmarks.jar.phase to package. -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <!-- pin to the version main already resolves by default (3.5.0), NOT #604's inline 3.4.2 which would downgrade the plugin repo-wide. One plugin builds both the main jar and the benchmarks classifier, so they necessarily share this version; pinning here also supplies it to the bare provider declarations. -->
                    <version>3.5.0</version>
                    <executions>
                        <execution>
                            <id>benchmarks-classifier-jar</id>
                            <phase>${benchmarks.jar.phase}</phase>
                            <goals>
                                <goal>test-jar</goal>
                            </goals>
                            <configuration>
                                <classifier>benchmarks</classifier>
                                <includes>
                                    <include>**/*Benchmark*.class</include>
                                    <include>**/jmh_generated/**</include>
                                    <include>META-INF/BenchmarkList</include>
                                    <include>META-INF/CompilerHints</include>
                                </includes>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <!-- version pinning only; the benchmarks-jar guard execution lives in the root build/plugins so it applies to every module automatically -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-antrun-plugin</artifactId>
                    <version>3.1.0</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!-- benchmarks-jar guard: runs for every module. Enforces on providers (benchmarks.expected.package set), and also fails any module that builds a jar of runnable benchmarks without declaring that property. A no-op on modules that build no benchmarks jar. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>verify-benchmarks-jar</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target xmlns:if="ant:if" xmlns:unless="ant:unless">
                                <!-- tests (and thus the benchmarks jar) are skipped entirely under -Dmaven.test.skip=true; there is nothing to verify, so the whole guard no-ops -->
                                <condition property="benchmarksGuardSkip">
                                    <istrue value="${maven.test.skip}"/>
                                </condition>
                                <property name="benchmarksJar" value="${project.build.directory}/${project.build.finalName}-benchmarks.jar"/>
                                <available file="${benchmarksJar}" property="benchmarksJarPresent"/>
                                <!-- fresh = jar exists and is newer than the compiled test-classes, i.e. built this run (a stale jar from an earlier build does not count) -->
                                <uptodate property="benchmarksJarFresh" targetfile="${benchmarksJar}">
                                    <srcfiles dir="${project.build.testOutputDirectory}" includes="**/*" erroronmissingdir="false"/>
                                </uptodate>
                                <!-- read the index only when a jar is present, so modules that build none never hard-error here -->
                                <loadresource property="benchmarkList" quiet="true" if:set="benchmarksJarPresent">
                                    <zipentry zipfile="${benchmarksJar}" name="META-INF/BenchmarkList"/>
                                </loadresource>
                                <!-- allowlist: the jar must contain ONLY our own classes + JMH/Maven metadata. Count any file entry outside that set; a third-party lib (org/**, io/**, ...) would land here. zipfileset yields only file entries, so bare directory entries never false-positive. -->
                                <resourcecount property="forbiddenEntryCount" if:set="benchmarksJarPresent">
                                    <zipfileset src="${benchmarksJar}">
                                        <exclude name="com/salesforce/multicloudj/**"/>
                                        <exclude name="META-INF/**"/>
                                    </zipfileset>
                                </resourcecount>
                                <fail message="${project.artifactId}: benchmarks jar contains ${forbiddenEntryCount} entries outside com/salesforce/multicloudj and META-INF - refusing to publish non-benchmark content" unless:set="benchmarksGuardSkip">
                                    <condition>
                                        <and>
                                            <isset property="forbiddenEntryCount"/>
                                            <not><equals arg1="${forbiddenEntryCount}" arg2="0"/></not>
                                        </and>
                                    </condition>
                                </fail>
                                <!-- completeness: every benchmark-pattern class compiled for this module must survive into the jar. If the include filter regresses (a class pattern dropped), compiled count exceeds jar count and we refuse to publish an incomplete bundle - this covers abstract bases that runnable benchmarks extend, so a filter regression cannot ship an unrunnable jar. Only class-bearing patterns are compared here; META-INF metadata is checked by the BenchmarkList guards below. Gated on Fresh so the counts reflect this run, and the jar (built from testOutputDirectory via the filter) is always a subset, making equality the correctness condition. -->
                                <resourcecount property="compiledBenchmarkClassCount" if:set="benchmarksJarFresh">
                                    <fileset dir="${project.build.testOutputDirectory}" erroronmissingdir="false">
                                        <include name="**/*Benchmark*.class"/>
                                        <include name="**/jmh_generated/**"/>
                                    </fileset>
                                </resourcecount>
                                <resourcecount property="jarBenchmarkClassCount" if:set="benchmarksJarFresh">
                                    <zipfileset src="${benchmarksJar}">
                                        <include name="**/*Benchmark*.class"/>
                                        <include name="**/jmh_generated/**"/>
                                    </zipfileset>
                                </resourcecount>
                                <fail message="${project.artifactId}: benchmarks jar has ${jarBenchmarkClassCount} benchmark classes but ${compiledBenchmarkClassCount} were compiled - the include filter dropped classes; refusing to publish an incomplete bundle" unless:set="benchmarksGuardSkip">
                                    <condition>
                                        <and>
                                            <isset property="benchmarksJarFresh"/>
                                            <not><equals arg1="${compiledBenchmarkClassCount}" arg2="${jarBenchmarkClassCount}"/></not>
                                        </and>
                                    </condition>
                                </fail>
                                <!-- provider declared intent but no fresh jar was built (phase not set to package, or bare maven-jar-plugin not declared) -->
                                <fail message="${project.artifactId}: benchmarks.expected.package is set but no fresh benchmarks jar was built - set benchmarks.jar.phase to package and declare a bare maven-jar-plugin" unless:set="benchmarksGuardSkip">
                                    <condition>
                                        <and>
                                            <isset property="benchmarks.expected.package"/>
                                            <not><isset property="benchmarksJarFresh"/></not>
                                        </and>
                                    </condition>
                                </fail>
                                <!-- provider jar built but its BenchmarkList is missing/empty (zero runnable benchmarks) -->
                                <fail message="${project.artifactId}: benchmarks jar has a missing or empty BenchmarkList - zero benchmarks" unless:set="benchmarksGuardSkip">
                                    <condition>
                                        <and>
                                            <isset property="benchmarks.expected.package"/>
                                            <not><isset property="benchmarkList"/></not>
                                        </and>
                                    </condition>
                                </fail>
                                <!-- provider jar built but does not contain this module's own benchmarks -->
                                <fail message="${project.artifactId}: benchmarks jar is missing ${benchmarks.expected.package} benchmarks" unless:set="benchmarksGuardSkip">
                                    <condition>
                                        <and>
                                            <isset property="benchmarks.expected.package"/>
                                            <isset property="benchmarkList"/>
                                            <not><contains string="${benchmarkList}" substring="${benchmarks.expected.package}"/></not>
                                        </and>
                                    </condition>
                                </fail>
                                <!-- a module shipped runnable benchmarks but never declared benchmarks.expected.package, so the guard could not verify it -->
                                <fail message="${project.artifactId}: benchmarks jar contains runnable benchmarks but benchmarks.expected.package is not set - declare it so the guard verifies this module" unless:set="benchmarksGuardSkip">
                                    <condition>
                                        <and>
                                            <not><isset property="benchmarks.expected.package"/></not>
                                            <isset property="benchmarkList"/>
                                        </and>
                                    </condition>
                                </fail>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <release>${releaseTarget}</release>
                    <!--
                      JDK 23+ no longer runs annotation processors discovered
                      only on the classpath by default, so every processor the
                      build relies on must be listed explicitly here. lombok
                      generates boilerplate; google auto-service generates the
                      META-INF/services entries that ServiceLoader uses for
                      provider discovery.
                    -->
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>com.google.auto.service</groupId>
                            <artifactId>auto-service</artifactId>
                            <version>${auto-service.version}</version>
                        </path>
                        <path>
                            <groupId>org.openjdk.jmh</groupId>
                            <artifactId>jmh-generator-annprocess</artifactId>
                            <version>${jmh.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-site-plugin</artifactId>
                <version>3.21.0</version>
                <configuration>
                    <skipDeploy>true</skipDeploy>
                </configuration>
                <inherited>false</inherited>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.3.0</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>tree</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>2.5.0</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>aggregate-add-third-party</goal>  <!-- Generates license report -->
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.7</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- Checkstyle Plugin for Code Quality - enforces Google Java Style -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <configLocation>config/checkstyle/checkstyle.xml</configLocation>
                    <suppressionsLocation>config/checkstyle/checkstyle-suppressions.xml</suppressionsLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                    <failOnViolation>true</failOnViolation>
                    <violationSeverity>error</violationSeverity>
                    <includeTestSourceDirectory>true</includeTestSourceDirectory>
                </configuration>
                <executions>
                    <execution>
                        <id>validate</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>10.25.1</version>
                    </dependency>
                </dependencies>
            </plugin>
            
            <!-- Source and Javadoc plugins for all modules -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>2.2.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.11.2</version>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            
            <!-- Maven Flatten Plugin to resolve ${revision} property -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>1.5.0</version>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>oss</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>release</id>
            <distributionManagement>
                <snapshotRepository>
                    <id>central-mj</id>
                    <url>https://central.sonatype.com/api/v1/publisher/deployments</url>
                </snapshotRepository>
                <repository>
                    <id>central-mj</id>
                    <url>https://central.sonatype.com/api/v1/publisher/deployments</url>
                </repository>
            </distributionManagement>
            <build>
                <plugins>
                    <!-- GPG signing -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.0</version>
                        <configuration>
                            <gpgArguments>
                                <arg>--pinentry-mode</arg>
                                <arg>loopback</arg>
                            </gpgArguments>
                            <keyname>2C0531E36F56DB90</keyname>
                        </configuration>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Nexus Staging Plugin -->
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.4.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central-mj</publishingServerId>
                            <tokenAuth>true</tokenAuth>
                            <autoPublish>true</autoPublish>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!--
          Benchmarks run under -DrunBenchmarks=true. The JaCoCo agent instruments every class in
          the forked JMH JVM, which distorts throughput/latency measurements (observed ~40% lower).
          Auto-activate on that property to skip coverage instrumentation, so a benchmark run is
          never silently measured with the agent attached. No effect on normal test/verify runs.
        -->
        <profile>
            <id>benchmark</id>
            <activation>
                <property>
                    <name>runBenchmarks</name>
                    <value>true</value>
                </property>
            </activation>
            <properties>
                <jacoco.skip>true</jacoco.skip>
            </properties>
        </profile>
    </profiles>

    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-project-info-reports-plugin</artifactId>
                <version>3.4.2</version>  <!-- Use a stable version -->
                <inherited>false</inherited>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-report-plugin</artifactId>
                <version>3.4.0</version>
                <configuration>
                    <aggregate>true</aggregate>
                </configuration>
                <inherited>false</inherited>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.11.2</version>
                <reportSets>
                    <reportSet>
                        <id>aggregate</id>
                        <inherited>false</inherited>
                        <reports>
                            <report>aggregate</report>
                        </reports>
                        <configuration>
                            <sourcepath>registry/registry-client/target/generated-sources/delombok:registry/registry-aws/target/generated-sources/delombok:registry/registry-gcp/target/generated-sources/delombok:dbbackuprestore/dbbackuprestore-client/target/generated-sources/delombok:dbbackuprestore/dbbackuprestore-aws/target/generated-sources/delombok:dbbackuprestore/dbbackuprestore-ali/target/generated-sources/delombok:dbbackuprestore/dbbackuprestore-gcp-firestore/target/generated-sources/delombok:blob/blob-client/target/generated-sources/delombok:blob/blob-aws/target/generated-sources/delombok:blob/blob-gcp/target/generated-sources/delombok:blob/blob-ali/target/generated-sources/delombok:sts/sts-client/target/generated-sources/delombok:iam/iam-client/target/generated-sources/delombok:iam/iam-gcp/target/generated-sources/delombok:iam/iam-aws/target/generated-sources/delombok:docstore/docstore-client/target/generated-sources/delombok:docstore/docstore-aws/target/generated-sources/delombok:docstore/docstore-gcp-firestore/target/generated-sources/delombok:docstore/docstore-gcp-spanner/target/generated-sources/delombok:docstore/docstore-ali/target/generated-sources/delombok:pubsub/pubsub-client/target/generated-sources/delombok:multicloudj-common/target/generated-sources/delombok:examples/target/generated-sources/delombok</sourcepath>
                        </configuration>
                    </reportSet>
                </reportSets>
            </plugin>
        </plugins>
    </reporting>

</project>